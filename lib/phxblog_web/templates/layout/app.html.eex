<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Phxblog!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          
    <%= if Phxblog.Auth.user_signed_in?(@conn) do %>
      <nav class="my-2 my-md-0 mr-md-3">
        Signed in as: <strong><%= Phxblog.Auth.current_user(@conn).username %></strong>
      </nav>
    <% end %>
  
  
  <%= if Phxblog.Auth.user_signed_in?(@conn) do %>
    <nav class="my-2 my-md-0 mr-md-3">
      Signed in as: <strong><%= Phxblog.Auth.current_user(@conn).username %></strong>
    </nav>
    <%= link "Sign Out", to: session_path(@conn, :delete), method: :delete, class: "btn btn-outline-primary" %>
    <% end %>
  
    <%= link "Sign In",
    to: session_path(@conn, :new),
    class: "btn btn-outline-primary" %>
    
    <%=  
       link "Sign up",
       to: registration_path(@conn, :new),
       class: "btn primary-btn"

       %>

        </nav>
      <!--  <span class="logo"></span> -->

      </header>


      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
